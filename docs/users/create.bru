meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{hostname}}/users
  body: json
  auth: inherit
}

body:json {
  {
      "name": "{{$randomFullName}}",
      "email": "{{$randomEmail}}"
  }
}

settings {
  encodeUrl: true
}
